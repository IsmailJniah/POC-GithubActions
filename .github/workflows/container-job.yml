#Run a job in a container.

on: [push]


name: container

jobs:
  node-docker:
    container:
      image: ubuntu:latest
    steps:
         - name: Log the node version
           run: |
             node -v
             cat /etc/os-release  
